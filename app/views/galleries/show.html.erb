<div class="container mx-auto px-4 py-8">
  <div class="mb-6">
    <%= link_to "&larr; Back to Galleries".html_safe, galleries_path, class: "text-amber-700 hover:text-amber-800 transition-colors font-medium" %>
  </div>

  <div class="flex items-start justify-between mb-6">
    <div>
      <h1 class="font-marcellus text-4xl text-stone-900 mb-2"><%= @gallery.title %></h1>
      <% if @gallery.description.present? %>
        <p class="text-stone-600 max-w-2xl"><%= @gallery.description %></p>
      <% end %>
    </div>
    <% if authenticated? %>
      <div class="flex gap-2 shrink-0 ml-4">
        <%= link_to "Edit", edit_gallery_path(@gallery), class: "rounded-md px-4 py-2 bg-stone-700 hover:bg-stone-600 text-white font-medium transition-colors" %>
        <%= button_to "Delete", gallery_path(@gallery), method: :delete,
              class: "rounded-md px-4 py-2 bg-red-700 hover:bg-red-600 text-white font-medium cursor-pointer transition-colors",
              data: { turbo_confirm: "Are you sure you want to delete this gallery?" } %>
      </div>
    <% end %>
  </div>

  <% if @gallery_images.any? %>
    <div class="grid grid-cols-2 sm:grid-cols-3 md:grid-cols-4 gap-4"
         data-controller="lightbox"
         role="list"
         aria-label="<%= @gallery.title %> images">
      <% @gallery_images.each_with_index do |gi, index| %>
        <div role="listitem">
          <button type="button"
                  class="block w-full aspect-square overflow-hidden hover:opacity-90 focus:outline-none focus:ring-2 focus:ring-amber-500 focus:ring-offset-2 transition-opacity cursor-pointer"
                  data-lightbox-target="item"
                  data-lightbox-src="<%= gi.image.display_url(width: 1600) %>"
                  data-lightbox-alt="<%= gi.image.alt_text.presence || gi.image.title.presence || "" %>"
                  data-action="click->lightbox#open"
                  aria-label="<%= "View #{gi.image.title.presence || "image #{index + 1}"} — click to enlarge" %>">
            <img src="<%= gi.image.thumbnail_url(width: 800, height: 800) %>"
                 alt=""
                 aria-hidden="true"
                 class="w-full h-full object-cover">
          </button>
        </div>
      <% end %>
    </div>
  <% else %>
    <p class="text-stone-600">No images in this gallery yet.</p>
  <% end %>
</div>
